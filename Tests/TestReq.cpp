#include "TestReq.h"

namespace TestReduce
{
    /*bool operator == (TestReq const& lhs, TestReq const& rhs)
	{
		return lhs.id() == rhs.id();
	}

	bool operator < (TestReq const& lhs, TestReq const& rhs)
	{
		return lhs.id() < rhs.id();
	}

	bool operator > (TestReq const& lhs, TestReq const& rhs)
	{
		return !(lhs < rhs);
	}*/
    
}	//namespace TestReduce

